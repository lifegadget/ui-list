{{#if hasBlock}}
  {{yield this}}
{{else}}
  {{#each aspects as |aspect|}}
    {{component aspect
      pane=this
      options=this.options
      format=this.format
      value=value
    }}
  {{/each}}
{{/if}}
