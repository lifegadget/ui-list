{{#each arrangedContent as |item|}}
    {{ui-item 
        list=this
        aspectPanes=item
        map=mappedProperties
        defaultValues=defaultValues
        keyAspectPanes=keyAspectPanes
        size=size 
        style=style
        mood=mood
        squeezed=squeezed
    }}
{{/each}}

{{yield}}
