{{#each arrangedContent as |item|}}
    {{ui-item 
        list=this
        data=item
        map=mappedProperties
        defaultValues=defaultValues
        _itemSetProperties=_itemSetProperties
        size=size 
        style=style
        mood=mood
        squeezed=squeezed
    }}
{{/each}}

{{yield}}
