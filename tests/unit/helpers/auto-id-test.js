import { autoId } from 'dummy/helpers/auto-id';
import { module, test } from 'qunit';

module('Unit | Helper | auto id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = autoId([42]);
  assert.ok(result);
});
