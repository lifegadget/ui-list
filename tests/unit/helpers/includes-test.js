import { includes } from 'dummy/helpers/includes';
import { module, test } from 'qunit';

module('Unit | Helper | includes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = includes([42]);
  assert.ok(result);
});
